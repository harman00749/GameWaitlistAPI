# GameWaitlistAPI
Game Waitlist CRUD API is a Node.js and Express REST API for managing game floor waitlist entries. It supports GET, POST, PUT, DELETE routes with route parameters, validation, XSS sanitization, standardized JSON responses, loading states, empty states, analytics logs, RAWG poster lookup, and an accessible staff interface.
